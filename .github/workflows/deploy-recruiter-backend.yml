# Deploy Recruiter Backend to Dokploy
name: Deploy Recruiter Backend

on:
  push:
    branches: [main]
    paths:
      - 'recruiter/backend/**'
      - '.github/workflows/deploy-recruiter-backend.yml'
  workflow_dispatch:

env:
  APP_NAME: recruiter-backend
  APP_PATH: recruiter/backend

jobs:
  deploy:
    name: Deploy to Dokploy
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Trigger Dokploy Deployment
        run: |
          curl -X POST "${{ secrets.DOKPLOY_URL }}/api/application.redeploy" \
            -H "Authorization: Bearer ${{ secrets.DOKPLOY_TOKEN }}" \
            -H "Content-Type: application/json" \
            -d '{"applicationId": "${{ secrets.RECRUITER_BACKEND_APP_ID }}"}'
          
          echo "Deployment triggered for ${{ env.APP_NAME }}"

      - name: Deployment Status
        if: success()
        run: echo "✅ ${{ env.APP_NAME }} deployment triggered successfully"
