# Deploy LMS to Dokploy
name: Deploy LMS

on:
  push:
    branches: [main]
    paths:
      - "lms/**"
      - ".github/workflows/deploy-lms.yml"
  workflow_dispatch:

env:
  APP_NAME: lms
  APP_PATH: lms

jobs:
  deploy:
    name: Deploy to Dokploy
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Validate Required Secrets
        run: |
          missing=0
          for var in DOKPLOY_URL DOKPLOY_TOKEN LMS_APP_ID; do
            if [ -z "${!var}" ]; then
              echo "[ERROR] Missing required secret: $var"
              missing=1
            fi
          done
          if [ "$missing" -ne 0 ]; then
            echo "Set the missing secrets in GitHub repo settings before deploying."
            exit 1
          fi
        env:
          DOKPLOY_URL: ${{ secrets.DOKPLOY_URL }}
          DOKPLOY_TOKEN: ${{ secrets.DOKPLOY_TOKEN }}
          LMS_APP_ID: ${{ secrets.LMS_APP_ID }}

      - name: Trigger Dokploy Deployment
        run: |
          response=$(curl -s -w "%{http_code}" -o /tmp/response.txt -X POST "${{ secrets.DOKPLOY_URL }}/api/application.deploy" \
            -H "x-api-key: ${{ secrets.DOKPLOY_TOKEN }}" \
            -H "Content-Type: application/json" \
            -H "accept: application/json" \
            -d "{\"applicationId\": \"${{ secrets.LMS_APP_ID }}\"}")

          if [ "$response" -eq 200 ]; then
            echo "[OK] ${{ env.APP_NAME }} deployment triggered successfully (HTTP $response)"
            cat /tmp/response.txt 2>/dev/null || true
          else
            echo "[ERROR] Deployment failed with HTTP status code: $response"
            cat /tmp/response.txt 2>/dev/null || true
            exit 1
          fi
