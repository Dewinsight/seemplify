# Private mail access runbook template

Keep the populated copy only in the ignored `access` folder. Record references
to the password manager or protected platform fields, not plaintext secrets.

- Dokploy project/environment:
- Dokploy compose ID:
- Dokploy compose URL:
- Dokploy server SSH user/host and key reference:
- Production Cloudflare tunnel ID/name:
- Staging Cloudflare tunnel ID/name:
- Public hostname and tunnel CNAME target:
- Google Workspace relay account and app-password vault reference:
- Mail API key IDs, scopes, product owners and secret-store references:
- R2 bucket/endpoint, token reference and offline age identity custodian:
- Last tested backup object and restore date:
- Migration snapshot path and 30-minute rollback deadline:
- Old local tunnel connector name (until cleanup):
