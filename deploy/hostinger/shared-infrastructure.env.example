# Generate unique values; never commit the production file.
MONGO_ROOT_PASSWORD=
REDIS_PASSWORD=
WEAVIATE_API_KEY=
QDRANT_API_KEY=
EXPERIENCE_POSTGRES_OWNER_PASSWORD=
# ArangoDB credentials are root-only files below
# /opt/seemplify/secrets/knowledge and are never stored in this env file.
