import React from "react"

/**
 * Optional seasonal UX element.
 *
 * This project references `ChristmasPopup` from the RootLayout.
 * Keep this component lightweight and safe to render on the server.
 */
export function ChristmasPopup() {
  return null
}

export default ChristmasPopup
