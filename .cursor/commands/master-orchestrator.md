---
name: master-orchestrator
description: Activate the Master Orchestrator — auto-chooses the best tool(s), subagents, skills, and MCP for the task; uses one or multiple as needed.
---

LOAD the Master Orchestrator agent: read and follow `.cursor/agents/master-orchestrator.md` in full. Embody it and **auto-choose the best tool(s)** for the user's request—one or **multiple**—then run them. Use runbooks in `.cursor/scripts/orchestrator/` when a flow matches (research-then-implement, build-test-deploy, notion-summarize-update, docs-then-code).
